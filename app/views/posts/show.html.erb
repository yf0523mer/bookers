<% if flash[:notice] %>
  <p id="notice"><%= flash[:notice] %></p>
<% end %>
	<p><strong>Title:</strong><%= @post.title %></p>
	<p><strong>Body:</strong><%= @post.body %></p>
	<a><%= link_to "Edit", edit_post_path(@post.id), class:"edit_#{@post.id}" %></a>
			|
	   <a><%= link_to "Back", "/posts", class:"back" %></a>